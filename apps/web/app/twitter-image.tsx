/**
 * X reads `twitter:image` before `og:image`; emitting both keeps the card the
 * same everywhere. Same image as the Open Graph one.
 */
export { alt, contentType, default, size } from "./opengraph-image";
